= simple_form_for(@organization) do |f|
  = f.input :name, :input_html => {:class => "flip"}, :required => false
  = f.input :public, :label => "Should this organization be public?"
  = f.submit :class => "button"
